
----------------------------------------------------------------

Feature: Make this run as a webservice.

Commit: Change (api-parse) to a simple string->string, and
        move the fun formatting to the REPL impl.

----------------------------------------------------------------

 Misc commits

Bug:    Words that are more than one syllable long aren't
        recognized.

Bug:    In the REPL, the "> " prompt can be deleted.

Commit: Make it spit out the actual different typelists for the
        different arities of the resulting predicates (may
        require minor or moderate restructuring of the code).

Commit: Make failed compositions show up as actual errors?
        (Might not be necessary.)

Feature: Add an "add-word" call to the API?